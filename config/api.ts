// src/config/api.ts 
const IS_DEV = __DEV__; 
 
// Теперь prod - это ваш VPS, dev - локальная разработка 
let API_BASE_URL = 'http://158.160.68.214:8000'; // ← это ваш PROD 
 
if (IS_DEV) { 
  // Для локальной разработки - локальный сервер 
  API_BASE_URL = 'http://158.160.68.214:8000'; 
   
  // Или если разрабатываете на физическом устройстве в той же сети 
  // API_BASE_URL = 'http://192.168.1.100:8000'; // IP вашего ноутбука 
} 
 
export { API_BASE_URL };
